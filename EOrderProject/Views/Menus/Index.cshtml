@model IEnumerable<Menu>


@{
    ViewData["Title"] = "Pizza Order";
}

<!-- ======= Hero Section ======= -->
@*<section id="hero">
    <div class="hero-container">
        <div id="heroCarousel" data-bs-interval="5000" class="carousel slide carousel-fade" data-bs-ride="carousel">
            <div class="carousel-inner" role="listbox">
                <div class="carousel-item active">
                    <img src="~/assets/img/slide/slide-4.jpg" class="img-responsive" />
                    <div class="carousel-container">
                        <div class="carousel-content">
                            <h2 class="animate__animated "><span>Order</span> Now</h2>
                            <p class="animate__animated">
                                Ut velit est quam dolor ad a aliquid qui aliquid. Sequi ea ut et est quaerat sequi nihil ut aliquam. Occaecati alias dolorem mollitia ut. Similique ea voluptatem. Esse doloremque accusamus repellendus deleniti vel. Minus
                                et tempore modi architecto.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>*@
<!-- End Hero -->


<main id="main">

    <section id="why-us" class="why-us">
        <div class="container mt-5">

            <div class="section-title justify-content-center pt-5">
                <h1>Check our tasty <span>Menu</span></h1>
            </div>

            <div class="row">
                <div class="col-lg-12 d-flex justify-content-center">
                    <ul id="menu-flters">
                        <li><a class="filter-active" asp-area="" asp-controller="Menus" asp-action="Index">Pizza</a></li>
                        <li><a asp-area="" asp-controller="Menus" asp-action="Fastfood">Fast Food</a></li>
                        <li><a asp-area="" asp-controller="Menus" asp-action="Pasta">Pastas</a></li>
                        @*<li><a asp-area="" asp-controller="Menus" asp-action="Salad">Salads</a></li>
                        <li><a asp-area="" asp-controller="Menus" asp-action="Drink">Drinks</a></li>*@
                    </ul>
                </div>
            </div>
          
            <table>
            <thead>
              <tr>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NameSortParm"]" asp-route-currentFilter="@ViewData["CurrentFilter"]">Name</a>
                <th>
                    <a asp-action="Index" asp-route-sortOrder="@ViewData["PriceSortParm"]" asp-route-currentFilter="@ViewData["CurrentFilter"]">Price</a>
                </th>
            </tr>
            </thead>
            </table>
           
            <div id="menu" class="row mt-4 justify-content-center">
                @if (TempData["AlertMessage"] != null)
                {
                    <div class="alert alert-success text-center">
                        @TempData["AlertMessage"]
                    </div>
                }

                @foreach (var item in Model)
                {

                    @if (item.MenuCategory == EOrderProject.Data.Enums.MenuCategory.Pizza)
                    {
                        <div class="col-lg-3 col-sm-5 m-1 mb-5 mt-2">
                        @*   @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                            {*@
                                <div class="p-2 text-white bg-dark rounded-top border border-bottom-0 border-secondary">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="text-white"><i class="bi bi-pencil-square"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" style="float: right !important;" class="text-white"><span>&times;</span></a>
                                </div>
                            @*}*@

                            <div class="box">
                                <div class="" style="position: relative; overflow: hidden; padding-bottom: 100%;">
                                    <img src="~/images/@Html.DisplayFor(modelItem => item.Image)" style=" position: absolute; max-width: 100%; max-height: 100%; top: 50%; left: 50%; transform: translateX(-50%) translateY(-50%);" alt="">
                                </div>
                                <h3 class="card-title mt-3">@Html.DisplayFor(modelItem => item.Name)</h3>
                                <p class="card-text mb-3"><i>@Html.DisplayFor(modelItem => item.Description)</i></p>
                                <h3 style="text-align: right;" class="card-text">@Html.DisplayFor(modelItem => item.Price)$</h3>
                               @* @if (User.Identity.IsAuthenticated)
                                {
                                    <a class="btn-order" asp-controller="Orders" asp-action="AddItemToShoppingCart" asp-route-id="@item.Id">Add to Cart</a>
                                }
                                else
                                {
                                    <a class="btn-order" asp-controller="Account" asp-action="Login">Add to Cart</a>
                                }*@


                            </div>
                        </div>
                    }
                }
            </div>

        </div>
    </section>
   @* @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
    {*@
        <div style="position: fixed; right: 25px; bottom: 25px;" class="text-white">
            <a asp-controller="Menus" asp-action="Create" class="btn-add-new"><i class="bi bi-plus-circle"></i> Add New </a>
        </div>
   @* }*@

</main>


